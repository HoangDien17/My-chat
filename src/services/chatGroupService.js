const userModel = require('../models/userModel');
const chatGroupModel = require('../models/chatGroupModel');

let addNewChatGroup = () => {
  return new Promise ((resolve, reject) => {
    
  })
};

module.exports = { addNewChatGroup };
